/**
 * Module 21 PATCH:
 * Wrap your app with <AppShell> to enable:
 * - Help Drawer
 * - Guided Tours
 * - Display toggles (large type / high contrast)
 *
 * Example:
 *   import { AppShell } from "@/components/shell/AppShell";
 *   ...
 *   <body> <AppShell>{children}</AppShell> </body>
 */
