/**
 * PATCH: Wrap AppShell contents with ToastRoot
 *
 * import { ToastRoot } from "@/components/toast/ToastProvider";
 *
 * export function AppShell({ children }) {
 *   return (
 *     <ToastRoot>
 *       <TourProvider> ... </TourProvider>
 *     </ToastRoot>
 *   );
 * }
 */
