/**
 * PATCH: In HelpDrawer topic rendering:
 *
 * {t.actions?.length ? (
 *   <div className="mt-3 flex flex-wrap gap-2">
 *     {t.actions.map((a, idx) => <HelpActionButton key={idx} action={a} />)}
 *   </div>
 * ) : null}
 */
