/**
 * Fundraising widget renderers
 * Uses the shared widgets from Module 4 (tiles, tables, charts).
 * If your registry renderer already picks the right renderer by widget type,
 * keep this file as a mapping layer for fundraising-specific formatting.
 */

export const FUNDRAISING_WIDGET_RENDERERS = {
  // intentionally empty for now; Module 4 renderer set should cover:
  // - KPI tiles (single-row)
  // - Tables (multi-row)
  // - Bar/line charts (series)
};
